kind = "web"
previewPath = "/"
title = "Qiskit Fall Fest 2026 — IISER TVM"
version = "1.0.0"
id = "artifacts/qiskit-fall-fest"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18427

[services.development]
run = "pnpm --filter @workspace/qiskit-fall-fest run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/qiskit-fall-fest", "run", "build" ]
publicDir = "artifacts/qiskit-fall-fest/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18427"
BASE_PATH = "/"
